/*!
 * Dark Mode Switch v1.0.1 (https://github.com/coliff/dark-mode-switch)
 * Copyright 2021 C.Oliff
 * Licensed under MIT (https://github.com/coliff/dark-mode-switch/blob/main/LICENSE)
 */

var darkSwitch = document.getElementById("checkbox");
window.addEventListener("load", function () {
    if (darkSwitch) {
        initTheme();
        darkSwitch.addEventListener("change", function () {
            resetTheme();
        });
    }
});

function initTheme() {
    var darkThemeSelected = localStorage.getItem("checkbox") !== null && localStorage.getItem("checkbox") === "dark";
    darkSwitch.checked = darkThemeSelected;
    darkThemeSelected ? document.body.setAttribute("data-theme", "dark") : document.body.removeAttribute("data-theme");
}

function resetTheme() {
    if (darkSwitch.checked) {
        document.body.setAttribute("data-theme", "dark");
        localStorage.setItem("checkbox", "dark");
    } else {
        document.body.removeAttribute("data-theme");
        localStorage.removeItem("checkbox");
    }
}

// Default dark theme
// resetTheme()

// Hide Spin Container
// function hideSpinContainer(){
//     var container = document.getElementById('spinContainer');
//     container.innerHTML = ''
// }

// setTimeout(hideSpinContainer, 2000)




